CRITICAL DIRECTIVE FOR ALL FUTURE ACTIONS:
Before you write any code, make any decisions, or proceed to the next steps, you must deeply analyze the 7 markdown files in the EL directory (00_MASTER_ARCHITECTURE.md to 06_EXPLAINER_AND_OUTPUTS.md).

The EL folder is the absolute ground truth for this project. Do not rely on standard boilerplate, general assumptions, or external logic. Every database schema, agent prompt, calculation, and workflow must be derived explicitly from those files.

Your Immediate Next Steps:

Execute the Phase 1 setup as approved.

Once Phase 1 is complete, read the entire EL directory.

Based strictly on the architecture and logic defined in the EL files, autonomously define the remaining project phases. Create markdown files for each phase (e.g., Phase_2_workflow.md, Phase_3_workflow.md) containing your task lists."